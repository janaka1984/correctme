pipeline {
  agent {label 'my-defined-label'}

stages {

stage('build') {
  steps {
         echo "building----"
             }
            }

stage('test') {
  steps {
         echo "testing-----"
             }
                     }
}
}
